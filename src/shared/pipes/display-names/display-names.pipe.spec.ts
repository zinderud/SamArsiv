import { DisplayNamesPipe } from './display-names.pipe';

describe('DisplayNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
